import { Component } from '@angular/core';
import {Employee} from './models/employee.model';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {
  title = 'my-app';
  // user: string;
  // x: number;
  // b: boolean;
  // a: any;
  // list: [];
  // listStrings: Array<string>;
  // employeeList: Array<Employee>;

  // other: Employee | any;
  // arrayNumbers = [1, 2, 3, 4];
  // arraystring: Array<string> = ['1', '2', '3'];

  // constructor() {
  //   this.employeeList = new Array<Employee>();


  //   this.employeeList.push(new Employee('Jesus', 'Sigala', 1, ['barrer']));
  //   this.employeeList.push(new Employee('Jairo', 'Gomez', 2, ['trapear']));
  //   this.employeeList.push(new Employee('Arian', 'Cortez', 3, ['cocinar']));
  // }
}
